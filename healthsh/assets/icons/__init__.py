"""SVG icon package. Files are loaded via ``importlib.resources``.

Icons are Tabler outline (MIT licensed; https://tabler.io/icons). The
:mod:`healthsh.ui.icons` module loads them and recolors via runtime QSS by
substituting the ``currentColor`` token.
"""
